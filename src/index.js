import React from 'react';
import ReactDOM from 'react-dom/client';
import SalaryCalculator from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SalaryCalculator />
  </React.StrictMode>
);
